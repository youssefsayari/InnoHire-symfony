<form method="post" action="{{ path('app_wallet_delete', {'id': wallet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wallet.id) }}">
    <button class="btn">Delete</button>
</form>
