{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_quiz</th>
                <td>{{ quiz.idQuiz }}</td>
            </tr>
            <tr>
                <th>Code_quiz</th>
                <td>{{ quiz.codeQuiz }}</td>
            </tr>
            <tr>
                <th>Nom_quiz</th>
                <td>{{ quiz.nomQuiz }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ quiz.description }}</td>
            </tr>
            <tr>
                <th>Prix_quiz</th>
                <td>{{ quiz.prixQuiz }}</td>
            </tr>
            <tr>
                <th>Image_quiz</th>
                <td>{{ quiz.imageQuiz }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_index') }}">back to list</a>

    <a href="{{ path('app_quiz_edit', {'id_quiz': quiz.id_quiz}) }}">edit</a>

    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
