<form method="post" action="{{ path('app_quiz_delete', {'id_quiz': quiz.id_quiz}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz.id_quiz) }}">
    <button class="btn">Delete</button>
</form>
